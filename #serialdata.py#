import serial
import time

ser = serial.Serial('/dev/cu.usbmodem141401', 115200)

input = ser.readLine()

count = 0

data = ""

while(count < 16):
    if stringInput[0] = '#':
        line += ser.readline() + '\n'

return line